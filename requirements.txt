# Core ML stack (used everywhere, including the offline demo)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Materials Project data + full Magpie featurization (needed for the REAL run).
# The demo and the fallback featurizer work without these.
mp-api>=0.41
pymatgen>=2024.0
matminer>=0.9
